{
  "name": "coingecko-mock-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "express": "^4.18.2",
    "validator": "^13.9.0"
  },
  "devDependencies": {
    "vitest": "^0.30.1"
  }
}