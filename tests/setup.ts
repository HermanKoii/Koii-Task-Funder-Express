// Global test setup file
import { beforeEach } from 'vitest';

// Reset global state or perform any necessary setup before each test
beforeEach(() => {
  // Clear mocks, reset any global state, etc.
});